# Server-only GitHub token with read:user access for private contribution counts.
# Never prefix this variable with NEXT_PUBLIC_.
GITHUB_TOKEN=

# Server-only Resend key scoped to the verified sending domain.
# Never prefix this variable with NEXT_PUBLIC_.
RESEND_API_KEY=re_replace_with_your_domain_scoped_sending_key
